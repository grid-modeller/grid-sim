# Pinned environment for the B6 two-zone evidence pack (venv pattern per
# scripts/era5-cf precedent; run with the era5 venv:
# ~/.local/share/grid-sim/era5-venv, Python 3.13.11). Parquet bytes
# (hence the committed manifest data/packs/b6.sha256) are conditional on
# these exact versions.
requests==2.34.2
pandas==3.0.3
pyarrow==24.0.0
openpyxl==3.1.5
numpy==2.5.0
